import SliderbedCalculatorApp from '../../components/SliderbedCalculatorApp';

/**
 * Slider Bed Conveyor Calculator Page
 *
 * This page renders the sliderbed calculator within the console shell.
 * All calculator logic is encapsulated in SliderbedCalculatorApp.
 */
export default function SliderbedPage() {
  return <SliderbedCalculatorApp />;
}
